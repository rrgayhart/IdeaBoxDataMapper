<div class="form">
  <ul class="form">
<form action="/idea/create" method="POST">
  <li>title
  <input type="text" name="title" id="title">
  </li>
  <li>
  description
  <input type="text" name="description" id="description">
  </li>
  <li>
  tags
  <input type="text" name="tag" id="tag">
</li>
  <input type="submit" value="Add Idea!" class="button"/>
</form>
</ul>
</div>


<% unless @ideas.empty? %>
  <h2>Ideas:</h2>
  <ul class="idealist">
    <% @ideas.each do |idea| %>
      <% unless idea.completed? %>
      <a href="/idea/<%= idea.id %>"> 
        <li class="ideaitem">
        <%= idea.title %>
        <div class="tab">
        <%= idea.description %>
        </div>
        </li>
      </a>
      <% end %>
    <% end %>
  </ul>
  <h2>Completed Ideas:</h2>
  <ul class="idealist">
  <% @ideas.each do |idea| %>
    <% if idea.completed? %>
      <a href="/idea/<%= idea.id %>"> 
        <li <%= "class=\"completed\"" if idea.completed? %>>
        <%= idea.title %>
        <div class="tab">
        <%= idea.description %>
        </div>
        </li>
      </a>
    <% end %>
  <% end %>
  </ul>
<% else %>
  <p>No Ideas!</p>
<% end %>
